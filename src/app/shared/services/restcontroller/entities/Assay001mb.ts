import { BaseEntity } from "./BaseEntity";

export class Assay001mb extends BaseEntity {
    ligandVersion?: number;
    // insertUser?: string;
    // insertDatetime?: Date;
    // updatedUser?: string | null;
    // updatedDatetime?: Date | null;
}